- 所有源码位于 src 目录下，通过 @ 别名引用，禁止相对路径跨层导入
- TypeScript 严格模式开启，isolatedModules 与 react-jsx 为强制编译选项
- 主进程与渲染进程通过 Electron IPC 通信，共享类型定义集中在 src/types/
- Canvas 渲染器与 store 中的布局常量（ROW_HEIGHT、LANE_WIDTH）分离职责：store 负责数据坐标计算，renderer 控制视觉渲染