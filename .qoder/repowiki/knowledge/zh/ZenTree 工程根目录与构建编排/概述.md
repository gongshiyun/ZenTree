ZenTree 仓库根目录，通过 package.json、tsconfig 与 vite.config 统一编排 Electron 主进程、Vite React 渲染进程、TypeScript 编译及 electron-builder 多平台打包流程。渲染层按 DDD 分层（domain / application / infrastructure / interfaces），Electron 主进程作为基础设施层提供 Git 与设置持久化能力。