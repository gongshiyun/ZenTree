- 每个 Wiki 页面以一级标题 `# Page Title` 开头，并使用二级/三级标题组织章节内容
- 页面间通过双括号 Wiki 链接语法 `[[PageName]]` 进行交叉引用，而非绝对路径
- 代码示例使用带语言标识的 fenced code block（如 ```typescript、```bash），并附带简要说明
- API 与常量通过表格形式呈现（方法名、参数、返回值、用途），保持结构化可读性
- 关键设计决策与注意事项使用 `>` 引用块或注释行标注，如 store 与 renderer 布局常量的差异说明